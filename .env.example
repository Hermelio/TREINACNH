# Django Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (MySQL)
DB_HOST=10.54.4.7
DB_PORT=3306
DB_NAME=Raio_X
DB_USER=integrador
DB_PASSWORD=crystalcomgas

# Mercado Pago (obtenha em https://www.mercadopago.com.br/developers)
# Use credenciais de TESTE (TEST-...) para desenvolvimento
# Use credenciais de PRODUÇÃO (APP_USR-...) para produção
MERCADOPAGO_PUBLIC_KEY=TEST-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
MERCADOPAGO_ACCESS_TOKEN=TEST-0000000000000000-000000-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx-000000000
# ID do collector: curl https://api.mercadopago.com/users/me -H "Authorization: Bearer SEU_TOKEN" | grep '"id"'
MERCADOPAGO_COLLECTOR_ID=0000000000

# URL base do site (usado nas back_urls do Mercado Pago e no webhook)
# Em desenvolvimento: http://localhost:8000
# Em produção: http://72.61.36.89:8080 (ou seu domínio)
SITE_URL=http://localhost:8000

# ==========================================
# EMAIL (Reset de senha e notificações)
# ==========================================
# Desenvolvimento: console (imprime no terminal, não envia de verdade)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# Produção: SMTP real
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=seu_email@gmail.com
EMAIL_HOST_PASSWORD=sua_app_password_aqui
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
DEFAULT_FROM_EMAIL=TreinaCNH <noreply@treinacnh.com.br>
SERVER_EMAIL=noreply@treinacnh.com.br
PASSWORD_RESET_TIMEOUT=86400

# allauth: protocolo dos links de reset (http em dev, https em produção)
ACCOUNT_DEFAULT_HTTP_PROTOCOL=http

# Security Headers (Production)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0
